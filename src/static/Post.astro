---
import type { Post } from '@wordpress/core-data';
import { getPost } from '@service/data';
import PostComponent from '@components/Post.svelte';

const { id } = Astro.props;

const post: Post = await getPost(id);
---

<PostComponent {...post} />
